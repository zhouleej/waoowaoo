# 2026-06-30 开发日志

## 工作概览

- 完成 `feature/platform-admin` 正式发行分支内容合并到 `wyk` 分支，并保持正式发行分支不变。
- 修复合并后 SaaS 相关数据库字段缺失导致的平台用户详情弹窗异常。
- 为阿里云百炼补齐图片生成模型配置、能力目录、价格目录与官方 provider 适配器实现。
- 修复百炼图片生成在角色资产、分镜图片生成链路中的参数协议、异步轮询与共享字段兼容问题。
- 修复分镜图片已经生成成功但历史失败提示仍遮挡图片的问题。

## 主要变更

### 平台管理与 SaaS Schema

- 新增 SaaS schema 健康检查脚本 `scripts/check-saas-schema.ts`。
- 新增 `npm run check:saas-schema`，用于检查关键 SaaS 表、字段、索引与默认套餐数据是否完整。
- 对合并后本地数据库缺少 `organization_members.quotaUsed` 等字段的问题进行迁移同步与风险检查。

### 阿里云百炼图片生成

- 在阿里云百炼官方模型目录中加入图片模型：
  - `wan2.6-t2i`
  - `wan2.5-t2i-preview`
  - `wan2.2-t2i-flash`
  - `wan2.2-t2i-plus`
  - `wanx2.1-t2i-turbo`
  - `wanx2.1-t2i-plus`
- 在 API 配置页预设模型中增加百炼图片模型选项。
- 在能力目录与价格目录中补齐百炼图片模型配置。
- 实现 `generateBailianImage`：
  - 支持 DashScope 异步提交。
  - 返回标准 `BAILIAN:IMAGE:{taskId}` externalId。
  - 根据模型区分新旧图片生成协议。
  - 支持 `aspectRatio` 到百炼尺寸参数的映射。
  - 兼容共享图片生成链路传入的 `referenceImages` 字段并忽略该字段。

### 百炼异步结果轮询

- 扩展百炼异步查询结果解析，支持新版图片生成结果中的 `output.choices[].message.content[].image`。
- 保持旧版 `output.results[].url` 兼容。

### 分镜图片体验修复

- 修复分镜面板图片已经生成成功后，历史失败提示仍覆盖图片的问题。
- 调整 `ImageSection` 渲染优先级：当 `imageUrl` 已存在时优先显示图片，不再让残留 `failedError` 遮挡生成结果。
- 增加组件回归测试，确保图片存在时历史失败提示不会覆盖图片。

## 测试与验证

- 通过百炼图片 provider 单测：`tests/unit/providers/bailian-image.test.ts`。
- 通过百炼能力目录测试：`tests/unit/model-capabilities/bailian-image-capabilities.test.ts`。
- 通过 API 配置页百炼预设模型测试：`tests/unit/api-config/preset-coming-soon.test.ts`。
- 通过分镜图片组件回归测试：`tests/unit/components/storyboard-image-section.test.ts`。
- 已执行并通过：
  - `npm run lint:all`
  - `npm run typecheck`

## 注意事项

- 开源本地模式如不需要计费，应保持 `.env` 中 `BILLING_MODE=OFF`，避免本地生成图片时触发余额不足。
- 百炼图片模型配置完成后，需要重启 Next.js dev 服务与 worker 服务，使 provider 配置和 worker 逻辑生效。
- 分镜图片历史失败任务记录可能仍存在数据库中，但已不会遮挡已有图片结果。
