# waoowaoo 项目解析

## 一、项目概述

**waoowaoo** 是一款基于 AI 技术的短剧/漫画视频制作工具，核心功能是将小说文本自动生成分镜脚本、角色形象、场景图片，并最终合成完整的视频内容。

项目地址: https://github.com/saturndec/waoowaoo

---

## 二、技术栈

### 2.1 前端技术

| 技术 | 版本 | 用途 |
|------|------|------|
| Next.js | 15.5.7 | React 全栈框架 (App Router) |
| React | 19.1.2 | UI 库 |
| Tailwind CSS | v4 | 样式框架 |
| TanStack Query | 5.90.20 | 服务端状态管理 |
| next-intl | 4.7.0 | 国际化 |
| next-auth | 4.24.11 | 认证 |
| Vercel AI SDK (ai) | 6.0.116 | AI 流式响应 |
| Remotion | 4.0.405 | 视频渲染 |
| Zod | 3.25.76 | 数据验证 |
| Lucide React | 0.575.0 | 图标库 |
| @dnd-kit | 6.3.1 | 拖拽交互 |

### 2.2 后端/服务端技术

| 技术 | 版本 | 用途 |
|------|------|------|
| Node.js | >=18.18.0 | 运行时 |
| Prisma | 6.19.2 | ORM |
| MySQL | 8.0 | 主数据库 |
| Redis | 7 | 缓存/消息队列 |
| BullMQ | 5.67.3 | 任务队列 |
| Express | 5.2.1 | Bull Board 管理界面 |

### 2.3 AI/模型集成

| SDK | 用途 |
|-----|------|
| @ai-sdk/google | Google AI (Gemini) |
| @ai-sdk/openai | OpenAI API |
| @google/genai | Google GenAI |
| @fal-ai/client | Fal.ai 平台 |
| @openrouter/sdk | OpenRouter 聚合 |
| 阿里云百炼 (bailian) | 自定义 provider |
| 硅基流动 (siliconflow) | 自定义 provider |

### 2.4 存储

| 方案 | 用途 |
|------|------|
| AWS S3 | 对象存储 |
| 腾讯云 COS | 对象存储 |
| MinIO | 本地 S3 兼容存储 |

### 2.5 开发工具

| 工具 | 用途 |
|------|------|
| TypeScript | 类型安全 |
| Vitest | 单元/集成测试 |
| ESLint | 代码检查 |
| Husky | Git hooks |
| tsx | TypeScript 执行 |
| concurrently | 并行开发脚本 |

---

## 三、系统架构

### 3.1 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        用户浏览器                            │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                     Next.js 应用服务器                       │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐ │
│  │  Pages/     │  │   API       │  │   SSE               │ │
│  │  App Router │  │   Routes    │  │   Server-Sent Events│ │
│  └─────────────┘  └─────────────┘  └─────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
         │                  │                    │
         ▼                  ▼                    ▼
┌─────────────────────────────────────────────────────────────┐
│              Worker 进程 (独立进程)                           │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ Image    │  │ Video    │  │ Voice    │  │ Text     │   │
│  │ Worker   │  │ Worker   │  │ Worker   │  │ Worker   │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
└─────────────────────────────────────────────────────────────┘
         │                  │                    │
         ▼                  ▼                    ▼
┌─────────────────────────────────────────────────────────────┐
│                    Redis (队列 + 缓存)                        │
└─────────────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────┐
│                       MySQL 数据库                           │
└─────────────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────┐
│          存储服务 (MinIO / S3 / COS)                         │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 服务部署架构 (Docker Compose)

```yaml
services:
  mysql:      # MySQL 8.0 (端口 13306)
  redis:      # Redis 7 (端口 16379)
  minio:      # MinIO 对象存储 (端口 19000/19001)
  app:        # Next.js 主应用 (端口 13000)
```

---

## 四、核心模块实现

### 4.1 任务系统 (Task System)

#### 任务类型定义 (`src/lib/task/types.ts`)

系统定义了丰富的任务类型，涵盖视频生成的完整流程：

```typescript
// 图片生成
IMAGE_PANEL, IMAGE_CHARACTER, IMAGE_LOCATION

// 视频生成
VIDEO_PANEL, LIP_SYNC

// 语音
VOICE_LINE, VOICE_DESIGN, VOICE_ANALYZE

// 内容处理
ANALYZE_NOVEL,           // 小说分析
STORY_TO_SCRIPT_RUN,     // 小说转脚本
SCRIPT_TO_STORYBOARD_RUN,// 脚本转分镜
CLIPS_BUILD,             // 构建片段
SCREENPLAY_CONVERT,      // 剧本转换

// AI 创作
AI_CREATE_CHARACTER,     // 创建角色
AI_CREATE_LOCATION,      // 创建场景
AI_MODIFY_APPEARANCE,    // 修改角色外观
AI_MODIFY_LOCATION,      // 修改场景
AI_MODIFY_PROP,          // 修改道具
AI_STORY_EXPAND,         // AI 故事扩展
```

#### 任务状态

```typescript
QUEUED -> PROCESSING -> COMPLETED | FAILED | CANCELED | DISMISSED
```

#### 任务队列

系统使用 BullMQ 创建 4 个独立队列：

| 队列 | 并发数配置 | 用途 |
|------|------------|------|
| image | 50 | 图片生成 |
| video | 50 | 视频生成 |
| voice | 20 | 语音合成 |
| text | 50 | 文本处理 (LLM) |

位置: `src/lib/task/queues.ts`

### 4.2 Worker 实现

Worker 入口: `src/lib/workers/index.ts`

```typescript
const workers = [
  createImageWorker(),   // 图片生成 Worker
  createVideoWorker(),   // 视频生成 Worker
  createVoiceWorker(),   // 语音 Worker
  createTextWorker()     // 文本处理 Worker
]
```

#### Image Worker (`src/lib/workers/image.worker.ts`)

处理所有图片生成任务:
- `IMAGE_CHARACTER`: 角色图片
- `IMAGE_LOCATION`: 场景图片
- `IMAGE_PANEL`: 分镜图片
- `ASSET_HUB_IMAGE`: 资产库图片

#### Video Worker (`src/lib/workers/video.worker.ts`)

处理视频相关任务:
- `VIDEO_PANEL`: 分镜视频生成
- `LIP_SYNC`: 唇形同步

#### Voice Worker (`src/lib/workers/voice.worker.ts`)

处理语音合成:
- `VOICE_LINE`: 对白生成
- `VOICE_DESIGN`: 声音设计
- `VOICE_ANALYZE`: 声音分析
- `ASSET_HUB_VOICE_DESIGN`: 资产库声音设计

#### Text Worker (`src/lib/workers/text.worker.ts`)

处理 LLM 调用任务:
- `ANALYZE_NOVEL`: 小说分析
- `STORY_TO_SCRIPT_RUN`: 小说转脚本
- `SCRIPT_TO_STORYBOARD_RUN`: 脚本转分镜
- 内容理解和生成相关任务

### 4.3 核心 Handler 实现

Handlers 目录: `src/lib/workers/handlers/`

| Handler | 功能 |
|---------|------|
| `analyze-novel.ts` | 解析小说文本，提取角色、场景、情节 |
| `story-to-script.ts` | 将小说转换为剧本格式 |
| `script-to-storyboard.ts` | 将剧本生成分镜脚本 |
| `character-image-task-handler.ts` | 生成角色图片 |
| `location-image-task-handler.ts` | 生成场景图片 |
| `panel-image-task-handler.ts` | 生成分镜图片 |
| `clips-build.ts` | 构建视频片段 |
| `voice-design.ts` | 设计角色语音 |
| `shot-ai-tasks.ts` | 分镜 AI 任务处理 |

### 4.4 API 路由结构

```
/api
├── /tasks              # 任务管理
├── /tasks/[taskId]     # 单个任务
├── /runs               # 运行管理
├── /runs/[runId]       # 单个运行
├── /sse                # Server-Sent Events
├── /projects           # 项目管理
├── /projects/[id]      # 项目详情
├── /assets             # 资产管理
├── /assets/[id]        # 资产详情
├── /auth               # 认证
│   └── [...nextauth]   # NextAuth 路由
├── /user               # 用户相关
│   ├── /balance        # 余额
│   ├── /costs          # 消费明细
│   ├── /models         # 模型配置
│   └── /transactions   # 交易记录
├── /asset-hub          # 资产库 (AI 设计)
│   ├── /characters     # 角色
│   ├── /locations      # 场景
│   ├── /voices         # 语音
│   ├── /generate-image # 生成图片
│   └── /voice-design   # 声音设计
└── /admin              # 管理后台
    └── /download-logs  # 日志下载
```

### 4.5 数据库模型 (Prisma Schema)

核心数据模型:

| Model | 用途 |
|-------|------|
| `User` | 用户账户 |
| `Account` | OAuth 账户关联 |
| `Project` | 项目 |
| `NovelPromotionProject` | 小说推文项目 |
| `NovelPromotionEpisode` | 剧集 |
| `NovelPromotionCharacter` | 角色 |
| `NovelPromotionLocation` | 场景 |
| `NovelPromotionClip` | 视频片段 |
| `NovelPromotionStoryboard` | 分镜 |
| `NovelPromotionPanel` | 分镜面板 |
| `NovelPromotionShot` | 镜头 |
| `NovelPromotionVoiceLine` | 台词/语音 |
| `CharacterAppearance` | 角色外观 |
| `LocationImage` | 场景图片 |
| `MediaObject` | 媒体对象 (图片/音频/视频) |
| `Task` | 任务记录 |
| `BillingLedger` | 计费账本 |
| `UserPreference` | 用户偏好 |

---

## 五、AI 集成架构

### 5.1 模型网关 (Model Gateway)

位置: `src/lib/model-gateway/`

系统实现了统一的模型网关，支持多供应商:

```typescript
// 模型路由
src/lib/model-gateway/router.ts

// LLM 调用
src/lib/model-gateway/llm.ts

// OpenAI 兼容接口
src/lib/model-gateway/openai-compat/

// 图片生成
src/lib/generators/image/

// 视频生成
src/lib/generators/video/

// 音频生成
src/lib/generators/audio/
```

### 5.2 支持的 AI Provider

#### LLM Providers

| Provider | 配置键 | 说明 |
|----------|--------|------|
| OpenAI | `OPENAI_API_KEY` | GPT 系列 |
| Google | `GOOGLE_API_KEY` | Gemini |
| 阿里百炼 | `BAILIAN_*` | 通义千问等 |
| 硅基流动 | `SILICONFLOW_*` | 第三方 API |
| OpenRouter | `OPENROUTER_API_KEY` | API 聚合 |

#### 图片生成

- OpenAI (DALL-E)
- Google (Gemini Image)
- Falcon (Fal.ai)
- Minimax
- 阿里百炼

#### 视频生成

- Google (Veo)
- OpenAI (Sora 兼容)
- Fal.ai
- Minimax
- 抖音 (vidu)

#### 语音合成

- 阿里百炼 TTS
- 硅基流动 TTS
- Fal.ai

---

## 六、计费系统

### 6.1 计费模式

```typescript
type BillingMode = 'OFF' | 'SHADOW' | 'ENFORCE'
```

- `OFF`: 关闭计费
- `SHADOW`: 影子模式 (记录但不扣除)
- `ENFORCE`: 强制计费

### 6.2 计费组件

位置: `src/lib/billing/`

| 模块 | 功能 |
|------|------|
| `ledger.ts` | 账本记录 |
| `cost.ts` | 成本计算 |
| `money.ts` | 金额处理 |
| `reporting.ts` | 报表生成 |
| `runtime-usage.ts` | 运行时用量 |
| `task-policy.ts` | 任务计费策略 |

---

## 七、核心功能流程

### 7.1 小说转视频完整流程

```
1. 上传小说 (分析)
   └─> ANALYZE_NOVEL
       - 提取角色
       - 提取场景
       - 解析情节

2. 角色创建
   └─> AI_CREATE_CHARACTER (生成角色图片)
   └─> 角色外观管理 (多个 appearance)

3. 场景创建
   └─> AI_CREATE_LOCATION (生成场景图片)

4. 小说转脚本
   └─> STORY_TO_SCRIPT_RUN
       - 分集
       - 生成分镜脚本

5. 脚本转分镜
   └─> SCRIPT_TO_STORYBOARD_RUN
       - 生成 storyboard
       - 生成 panel
       - 生成 shot

6. 分镜图片生成 (并行)
   └─> IMAGE_PANEL (每个 panel)
   └─> IMAGE_LOCATION (每个场景)
   └─> IMAGE_CHARACTER (每个角色外观)

7. 视频生成
   └─> VIDEO_PANEL
   └─> LIP_SYNC (唇形同步)

8. 语音合成
   └─> VOICE_DESIGN (设计声音)
   └─> VOICE_LINE (生成对白)
```

### 7.2 资产库 (Asset Hub)

独立于项目的全局资产管理系统:

- 全局角色库
- 全局场景库
- AI 设计和修改功能
- 跨项目复用

---

## 八、前端实现

### 8.1 页面结构

```
/app/[locale]/
├── page.tsx              # 首页/落地页
├── home/                 # 工作台首页
├── workspace/            # 工作区
│   └── asset-hub/        # 资产库
├── profile/              # 用户资料/设置
└── auth/
    ├── signin/           # 登录
    └── signup/           # 注册
```

### 8.2 国际化

支持语言: 中文 (zh), 英文 (en)

消息文件: `messages/{zh,en}/*.json`

### 8.3 状态管理

- React Query (TanStack Query) - 服务端状态
- React Context - UI 状态 (Toast, 等)
- Zustand 可选 - 客户端状态

---

## 九、开发与运维

### 9.1 开发模式

```bash
# 本地开发 (需要先启动 Docker 基础设施)
npm run dev

# 或使用 Docker 完整部署
docker-compose up -d
```

### 9.2 环境变量

关键配置项:

```env
# 数据库
DATABASE_URL=mysql://...

# Redis
REDIS_HOST=
REDIS_PORT=

# 存储
STORAGE_TYPE=minio|s3|cos|local
MINIO_ENDPOINT=
MINIO_BUCKET=

# 认证
NEXTAUTH_URL=
NEXTAUTH_SECRET=

# AI API Keys
OPENAI_API_KEY=
GOOGLE_API_KEY=
BAILIAN_API_KEY=
```

### 9.3 测试

```bash
# 单元测试
npm run test:unit:all

# 集成测试
npm run test:integration:api

# 完整测试
npm run test:all

# 代码检查
npm run lint
npm run typecheck
```

### 9.4 代码质量保障

项目包含多种 Guard 脚本 (`scripts/guards/`) 防止常见错误:
- API 路由契约检查
- Prompt 回归检查
- 模型配置检查
- 媒体规范化检查

---

## 十、总结

waoowaoo 是一个功能完整的 AI 视频创作平台，具有以下特点:

1. **完整的工作流**: 从小说输入到视频输出全自动处理
2. **多供应商集成**: 支持多种 AI 服务商，统一网关接口
3. **异步任务架构**: 使用 BullMQ 实现高效的异步任务处理
4. **模块化设计**: 任务类型、Handler、Provider 均高度模块化
5. **完善的计费系统**: 支持影子模式和强制计费
6. **多语言支持**: 中文/英文界面
7. **企业级部署**: Docker Compose 一键部署
8. **开发质量保障**: 完善的测试和代码检查体系

系统的核心价值在于降低视频内容创作的门槛，让用户只需提供小说文本，即可自动生成专业的视频内容。