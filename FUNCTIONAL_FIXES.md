# 功能修复与验收记录

## 2026-09-15 补充修复

已同步 `origin/feature/platform-admin` 的 `13efaf8`，保留主线新增的视频流播放、缩略图、移动云素材和防卡顿处理。本次没有修改数据库结构。

- 配音 Worker 接入取消检查；每次生成使用独立文件名，防止覆盖快照或旧剪辑引用的音频。
- 配音写回核对台词版本；期间编辑台词时拒绝写回旧声音。保存新音频同时清除旧口型引用。
- 口型接口与 Worker 都校验项目、剧集归属，覆盖既有队列任务。保存时锁定台词并检查视频与音频版本，阻止过期口型覆盖最新结果。
- 首尾帧视频保存前按固定顺序锁定首尾镜头并校验尾帧图片；尾帧变更后拒绝写回旧结果。
- 台词编辑与生成保存统一按“台词→镜头”更新，避免相反锁顺序；重新绑定时清理原镜头的口型。
- 灵感视频重试保留原任务的移动云素材 ID、参考图顺序和缩略图选项，不继承旧的运行标识。取消失败或进入结算时返回冲突，不再误报成功。

以上问题已增加回归测试，包含取消、输入中途变化、跨项目旧任务、尾帧变化、重试选项保留等场景。测试使用隔离依赖，不等同于真实数据库并发故障注入；没有调用付费模型。项目级音色绑定变更的完整依赖版本管理仍属于下面列出的扩展范围。

日期：2026-09-10。基线：264862c。修复分支：codex/functional-gap-fixes。

## 已实现

1. 局部分镜重试区分“不更新台词”和“清空台词”，更新原镜头记录，保留已有媒体和台词绑定。
2. 项目删除保留物理媒体，记录待回收清单，避免已发布资产图片被源项目删除连带损坏；远端音色清理放在数据库删除成功后。
3. 剧集替换先校验输入，在一个事务中完成删除、新建和项目元数据更新；同一项目导入加行锁。
4. 项目详情只返回创建者 ID、显示名和头像，不返回完整用户记录。
5. 手动修改台词内容、发言人、情绪或音色预设时清除旧音频引用和相关口型引用；视频重新生成清除旧口型和过期媒体 ID。
6. 接入剪辑入口、保存加载、后台渲染和成片下载；按镜头身份匹配多句台词，支持字幕与横竖屏。渲染使用保存时的快照，并验证素材属于当前剧集。
7. 剧本/分镜整体重生成前保存剧集快照，提供历史版本列表和事务恢复。恢复失败回滚；有运行中任务时拒绝恢复。
8. 浏览器批量下载重试失败项，未完整下载不生成成功 ZIP；服务端打包同样拒绝缺件成功。
9. 视频/配音批量提交分别返回已接收和失败项，前端显示失败原因并清除未接收台词的等待状态。
10. 组织管理员可按注册账号或邮箱添加成员；失败消息可见，禁止添加已禁用账号。
11. 阶段状态区分已有产物与全部完成。
12. 资产修订后重新发布创建新记录，保留旧记录；内容未变则复用原发布结果。

## 验证

- 每笔代码提交均通过仓库 pre-commit 要求的 lint、类型检查、测试守卫、单元测试、供应商契约、API 契约和生成链路测试；没有关闭提交钩子。
- 新增针对误删台词、导入失败回滚、账户字段、媒体保留、旧音频失效、批量部分失败、缺件下载、剪辑绑定/媒体范围、取消渲染、快照恢复及资产修订的测试。
- 实际使用 Remotion 导出双镜头 MP4，并解析视频/音频轨道和抽帧确认中文字幕。该检查使用本地合成素材，不调用付费模型。
- 已执行生产构建。Docker 命令在本机不可用，未进行镜像构建和容器端到端验收；Dockerfile 已补充浏览器与中文字体运行环境。
- 未使用真实支付、短信、邮件服务；没有对业务数据库执行破坏性测试。

## 当前边界与尚未实现的扩展

本轮完成上述具体修复和最小成片链路，不代表原分析报告的所有产品增强均已完成：

- 组织级模型目录、独立密钥与成员授权尚未实施，等待明确凭据归属；现有模型配置仍以个人配置为主。
- 添加已注册成员已打通；邮件邀请投递、受邀注册/接受链接、联系方式验证、密码找回尚未接入外部服务。
- 项目级查看/编辑/审核/发布角色、评论审批、负责人转移仍待设计实施。
- 在线支付、可信回调、自动续费、真实开票需要确定服务商和商业流程；现有管理员订单管理保持原有实现。
- 批次已支持部分成功反馈，但独立批次历史、整批预算锁定、预计耗时和按预算暂停尚未实现。
- 剪辑提供基础排序、转场和导出；专业裁剪、独立音频导入、BGM 编辑界面、成片版本列表仍需补齐。已有 BGM 数据可渲染，但媒体授权目前限当前剧集音频。
- 当前手动台词修改通过清除旧引用要求重新生成，没有完整的生成依赖版本图；音色绑定变更、运行中修改和其他生成写回仍需要扩展一致性策略。
- 快照是本地共享目录中的剧集版本，不是完整项目回收站；已被其他清理逻辑物理删除的历史媒体不能由快照重建。物理回收暂缓会增加存储占用。
- 验证包含隔离依赖的测试与实际渲染，不等同于真实数据库故障注入或完整浏览器创作验收。
