# Debug Session: billing-enterprise-empty

- 状态：OPEN
- 症状：平台“资费与订阅”页面中企业筛选和订阅数据无法显示。
- 约束：取得运行时证据前不修改业务逻辑。

## 可证伪假设

1. 企业下拉的数据 API 路径或响应字段与页面读取契约不一致。
2. 订阅 API 返回字段/分页结构与页面 `fetchTab` 解析不一致。
3. 平台管理员鉴权或全局锁定导致企业/订阅 API 返回 403/500。
4. 数据库中存在企业但没有订阅，页面未区分“无数据”和“加载失败”。
5. 最近订阅账期/项目外键 migration 后，相关 Prisma 查询因 schema 不同步失败。

## 运行时证据

- 数据库存在 1 个 active 企业。
- 数据库存在 2 个 active 套餐。
- `organization_subscriptions` 为 0 条；该企业 `currentSubscriptionId=null`。
- 因此订阅列表为空是数据事实，不是前端隐藏。
- 订阅 GET 原实现会捕获数据库异常并返回 HTTP 200 空数组，前端无法区分错误和空数据。
- 手工创建 active 订阅未传结束时间时会写入 `currentPeriodEnd=null`，与权益检查要求冲突。

## 根因判定

1. 企业响应主契约是 `{data,pagination}`，页面兼容读取但错误状态不足。
2. 订阅正常响应契约匹配；错误响应被伪装为空数据是缺陷。
3. 未发现管理员鉴权错误证据。
4. **确认**：当前确实无订阅，页面空态和创建引导不足。
5. 排除：Prisma 可正常读取企业、套餐和订阅表。

## 修复

- 订阅 GET 不再吞异常，统一返回非 2xx 错误。
- 手工订阅缺省按套餐月/年周期生成有效结束时间，支持月底与闰日钳制。
- 企业加载增加独立 loading/error/retry，严格解析 `{data}`。
- 订阅页显示企业、可用套餐、订阅数量。
- 企业选择项显示当前套餐/订阅状态或“未订阅”。
- 空态区分无企业、无套餐、有企业套餐但无订阅，并提供对应引导。
- 修复创建订阅后旧 tab 闭包导致刷新错误；创建成功后显式刷新 subscriptions 与企业状态。

## 验证

- TypeScript：通过。
- ESLint：通过。
- 订阅账期、空态和状态机测试：3 文件、15 项通过。
